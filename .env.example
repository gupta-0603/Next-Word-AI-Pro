# Copy this file to .env and adjust as needed.
# main.py doesn't read these yet by default (CORS/paths are hardcoded for
# simplicity) — wire them up with python-dotenv if you deploy publicly.

ENVIRONMENT=development
ALLOWED_ORIGINS=http://localhost:8000
LOG_LEVEL=INFO
